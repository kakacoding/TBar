# TBar
## 安装
- 在PackageManager中使用`Add package from git URL`菜单添加
- 添加宏 `TBAR`
## 
- 将`com.kakacoding.tbar`目录放在packages下
- 在`mainifest.json`中加入`"com.kakacoding.tbar": "file:com.kakacoding.tbar",`
- 添加宏 `TBAR`
